// Funciones// 
function ponele(param1,param2) {
    let a = param1 + " ama a " + param2;
    return a;
}
ponele("Juan", "Carla"); 

//Suma// 
function sumar (num1,num2) {
    return num1 + num2;
    }
    let resultado = sumar (5, 7);
    const duplicar = function(numero) {
        return numero * 2;
    }
    let resultado = numero(10);
    console.log(resultado);

//Duplicar// 
const duplicar = function(numero) {
    return numero * 2;
}
let resultado = Duplicar(10);
console.log(resultado);

const duplicar = numero => numero * 10
